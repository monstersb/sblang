# sblang

```
# ./configure
# make
# ./build/main -h
Usage:
      ./build/main -e -f [filename]

      -h:  help
      -e:  echo
      -t:  tokenize
      -p:  parse
      -r:  run
      -f:  read from file
      -l:  log level [0 - 3]
```
